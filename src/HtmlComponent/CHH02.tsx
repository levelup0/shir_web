/* eslint-disable @typescript-eslint/no-explicit-any */
export default function CHH02({ title }: any) {
    return (
        <h2 className="text-black text-[32px] font-semibold leading-[32px]">
            {title}
        </h2>
    );
}
